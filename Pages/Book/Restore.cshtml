@page "/Books/Restore/{id:int}"
@model LibraryManagementSystem.Pages.Books.RestoreModel
@{
    ViewData["Title"] = "Restore Book";
}

<div class="row justify-content-center">
    <div class="col-md-5">
        <div class="card shadow-sm border-success">
            <div class="card-header bg-success text-white fw-semibold">
                <i class="bi bi-arrow-counterclockwise me-2"></i>Restore Book
            </div>
            <div class="card-body text-center py-4">
                <p class="mb-1 text-muted">Restore this book to active?</p>
                <h5 class="fw-bold">@Model.Book.Title</h5>
                <p class="text-muted small">by @Model.Book.Author</p>
                <div class="d-flex justify-content-center gap-3 mt-3">
                    <form method="post">
                        <input type="hidden" name="id" value="@Model.Book.Id" />
                        <button type="submit" class="btn btn-success">
                            <i class="bi bi-arrow-counterclockwise me-1"></i>Yes, Restore
                        </button>
                    </form>
                    <a href="/Books" class="btn btn-outline-secondary">Cancel</a>
                </div>
            </div>
        </div>
    </div>
</div>